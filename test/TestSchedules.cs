using System.Reflection;

namespace test
{ 

[TestClass]
    public class TestEmployeeSchedule
    {
        public static string schedulePath;

        //deze niet kwinten. keep it commented//[TestMethod]
        //deze niet kwinten. keep it commented//public void TestIsValidDate_ValidDate_ReturnsTrue()
        //deze niet kwinten. keep it commented//{
        //deze niet kwinten. keep it commented//    string validDate = "01-01-2024";
        //deze niet kwinten. keep it commented//    bool result = EmployeeSchedule.IsValidDate(validDate);
        //deze niet kwinten. keep it commented//    Assert.IsTrue(result);
        //deze niet kwinten. keep it commented//}
    	//deze niet kwinten. keep it commented//
        //deze niet kwinten. keep it commented//[TestMethod]
        //deze niet kwinten. keep it commented//public void TestIsValidDate_InvalidDate_ReturnsFalse()
        //deze niet kwinten. keep it commented//{
        //deze niet kwinten. keep it commented//    string invalidDate = "32-01-2024";
        //deze niet kwinten. keep it commented//    bool result = EmployeeSchedule.IsValidDate(invalidDate);
        //deze niet kwinten. keep it commented//    Assert.IsFalse(result);
        //deze niet kwinten. keep it commented//}

        //[TestMethod]
        //public void TestGetUpdateSchedules()
        //{
        //// Arrange
        //ScheduleLogic scheduleLogic = new ScheduleLogic(schedulePath);
        //int newId = scheduleLogic.getID();
//
        //List<Dictionary<string, object>> listOfDicts = new();
//
        //ScheduleModel updateToAdd = new ScheduleModel(newId, "John", "01-01-2024", "8", "08:00", "16:00", new PerformanceModel(1, "Demo A", "Test description", DateTime.Now.AddDays(1), DateTime.Now.AddDays(2), 1, listOfDicts, true), true);
        //
        //// Act
        //scheduleLogic.UpdateList(updateToAdd);
//
        //// Assert
        //Assert.IsTrue(scheduleLogic.GetSchedules().Contains(updateToAdd));
        //}
    }
}